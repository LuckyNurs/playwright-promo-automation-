# Playwright Promo Automation (Example)

This is a **safe, example Playwright project** to demonstrate automating promo creation.

 **Important:** All URLs, usernames, passwords, and menu items are **dummy data**. Do **not** commit real credentials.

---

## Setup

1. Clone this repo:

```bash
git clone https://github.com/your-username/playwright-promo-automation.git
cd playwright-promo-automation
